name: faculty_attendance
description: Smart Faculty & Staff Attendance Management System
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  http: ^1.1.0
  geolocator: ^10.1.0
  camera: ^0.10.5+5
  intl: ^0.18.1
  google_fonts: ^6.1.0
  local_auth: ^2.1.6
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
